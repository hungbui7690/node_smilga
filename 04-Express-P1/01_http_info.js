/* 
  * Server và Client communicate thông qua HTTP Protocol >> vì vậy đc gọi là HTTP Message

  * Body: optional >> tùy vào từng trường hợp, ví dụ get thì không cần body, nhưng khi post thì phải có khi gửi request tới server
  * Status Code: signal of result

  * Vào đại page nào >> Network >> check trong đó sẽ thấy HTTP Msg
  * Sau khi click vào >> check Preview và Response Tab

?==========================================
?==========================================

  * Sau khi download start files >> xóa đi .git file để tránh trường hợp bị lỗi 
  * rmdir -Force -Recure .git
  * rd /s /q .git
  *
  * để chạy nhớ chạy npm install để cài packages (nodemon, express)
*/
